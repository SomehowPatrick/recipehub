generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model recipes {
  title       String   @id
  description String
  tags        String[]
  created     DateTime @db.Date
  ingredients String[]
  recipe_text String
}
